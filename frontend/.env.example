# DigitalOcean backend origin, no trailing slash.
# Leave empty on Vercel while vercel.json proxies /api to the DigitalOcean IP.
# After you add a backend domain with HTTPS, set:
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
